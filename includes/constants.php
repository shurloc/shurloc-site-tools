<?php
/**
 * Plugin constants.
 *
 * @package ShurlocSiteTools
 */

declare( strict_types=1 );

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

/**
 * Plugin version.
 */
define( 'SHURLOC_SITE_TOOLS_VERSION', '0.4.0.1' );

/**
 * Plugin directory path.
 */
define( 'SHURLOC_SITE_TOOLS_PATH', plugin_dir_path( __DIR__ ) );

/**
 * Plugin directory URL.
 */
define( 'SHURLOC_SITE_TOOLS_URL', plugin_dir_url( __DIR__ ) );
